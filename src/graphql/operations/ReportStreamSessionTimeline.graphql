mutation ReportStreamSessionTimeline($sessionId: ID!, $positionSeconds: Int!, $state: PlaybackState!) {
  reportStreamSessionTimeline(sessionId: $sessionId, positionSeconds: $positionSeconds, state: $state)
}
