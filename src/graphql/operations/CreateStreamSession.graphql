mutation CreateStreamSession($input: CreateStreamSessionInput!) {
  createStreamSession(input: $input) {
    session {
      id
      streamUrl
      transcodeMode
    }
    userErrors {
      __typename
      ... on MutationError {
        message
      }
    }
  }
}
